<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Auth theme. -->
    <style name="Theme.KaiCare" parent="Theme.Material3.Light.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_40</item>
        <item name="colorPrimaryVariant">@color/primary_10</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary_40</item>
        <item name="colorSecondaryVariant">@color/secondary_10</item>
        <item name="colorOnSecondary">@color/white</item>

    </style>

    <!-- Auth theme -->
    <style name="Theme.KaiCare.Auth" parent="Theme.KaiCare">
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Main theme -->
    <style name="Theme.KaiCare.Main" parent="Theme.KaiCare">
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
    </style>
</resources>